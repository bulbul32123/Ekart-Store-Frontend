
import '../globals.css'

export const metadata = {
  title: 'Special Product Discount',
  description: 'Get Special Products Discount from EKart. ',
}

export default function RootLayout({ children }) {
  return (
    <main>
        {children}
    </main>
  );
}
